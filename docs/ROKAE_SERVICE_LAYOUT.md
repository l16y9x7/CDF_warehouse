# ROKAE 按服务归档

按已确认的服务边界归档：`pose` 对应开机常驻的基础 SDK 与 8092 状态接口，`manipulation` 对应 `mui.target` 管理的 8082/8086/8091。相机服务不在本次范围，不改仓库 `camera` 目录；也不向 `agent`、`estimation` 或 `navigation` 拆放本项目代码。

## 来源与布局

- 主项目：AGX `/home/admin/mui/rokae_web_control`，2026-09-24 11:59:53 源码快照。
- 基础库：同机 `/home/admin/mui/arm_motion_control`，保持原代码。
- 共 170 个原文件，`pose` 59 个，`manipulation` 111 个。155 个主项目文件加 15 个配套库文件。
- `tools/rokae_bundle_manifest.json` 逐文件记录源路径、装配目标、原始 SHA-256 和执行权限。

以 SDK 常驻入口的本项目依赖为边界保留一份底层实现；业务入口、动作业务和网页属于 manipulation。共享 `rokae_web` 包分目录归档后通过构建恢复，不更改内部相对导入，也不重新拆分进程。现有服务继续使用原 AGX 路径，构建工具不自动部署。

原始快照混合 LF/CRLF，Git 属性仅对这次导入的目录关闭换行转换，确保取出后仍可用 `--verify-import` 校验。其他团队已有目录和属性不改。

## 排除范围

现场 `config.json`、三份标定、可编辑记忆点、日志/图片/录像、运行缓存、备份、厂家 SDK 和 URDF 不提交；保留示例配置及业务固定动作点位。

按本次范围排除独立相机驱动 `camera_right_rgb.py`、相机 service/权限安装脚本、手眼标定工具以及外部相机服务的协议测试。不引入独立 `vision` 项目源码。业务侧正常调用相机接口的代码和假硬件测试继续保留，以保持网页和扫码入口完整。

## 验证

- 170 个文件与现场当前源码及装配结果逐文件 SHA-256 一致。
- 构建器 5 项测试通过；常驻入口仅依赖 `pose` 源码即可导入；装配后的业务接口和网页入口导入通过，均未载入原生 SDK 或连接硬件。
- AGX 隔离目录完整 Python 回归 477 项：473 通过、4 历史失败、无跳过。测试不从现场主项目借用 Python 源码，少数 FK 测试只读现场 URDF。
- 13 组 Node.js 网页测试：12 通过、1 历史失败；Python、JavaScript 和 6 个 Shell 脚本语法检查通过。
- 3 项 Python 断言仍按旧 170 mm 工具长度计算，1 项仍判断软管不支持；`test_left_box_ui.js` 使用旧扫码 v4 状态。这些失败均已在未整理的原始源码复现。本次保留业务和原测试内容，不为归档修改动作参数。

隔离验证没有修改 AGX 的运行目录、网络或服务；没有执行实机动作。完整测试命令会报告历史失败，不能表述为全部回归通过。
