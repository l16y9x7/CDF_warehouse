# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# Prefer a conda env that already has these deps (e.g. nav) if you use one.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
numpy>=1.21,<2
opencv-python-headless==4.11.0.86
imageio-ffmpeg>=0.5
